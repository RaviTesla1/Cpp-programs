#include<stdio.h>
#include<conio.h>
main()
{
    int length,breadth;
    printf("Enter length and breadth of rectangle: ");
    scanf("%d%d",&length,&breadth);
    printf("\nlength of rectangle is %d",length*breadth);
    getch();
}
